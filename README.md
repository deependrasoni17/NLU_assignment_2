In problem 1 : the file named as Problem_1_NLU_A2.ipynb is code for problem 1. to run this file copy this to google colab and run the file, you need to upload a file in any cell, upload the .txt file file named as corpus.txt and wait till final run. finally you will see all the output in google colab content
In problem 1 : the file named as Problem_2_NLU_A2.ipynb is code for problem 2. to run this file copy this to google colab and run the file, you need to upload a file in any cell, upload the .txt file file named as india_names.txt and wait till final run. finally you will see all the output in google colab content
